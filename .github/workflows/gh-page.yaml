name: Registry

on:
  push:
    branches: [main]
    paths:
      - mkdocs.yaml
      - registry/**
      - docs/**
      - src/dataset_schema_registry/**
  pull_request:
    paths:
      - mkdocs.yaml
      - registry/**
      - docs/**
      - src/dataset_schema_registry/**
  schedule:
    - cron: '0 5 * * *'   # sync registry every day at 05:00 UTC
    - cron: '0 6 * * *'   # build docs every day at 06:00 UTC
  workflow_dispatch:

jobs:
  sync-registry:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    # Run on schedule (both crons trigger this, guard with time check is not
    # needed — GitHub deduplicates; simpler to just always run it on schedule
    # and dispatch).  Skip on plain push/PR to avoid unnecessary network calls.
    if: >-
      github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch'
    steps:
      - name: Check out the repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Configure git
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Install requirements
        run: uv sync

      - name: Sync dataset registry JSONcheck
        env:
          MDC_API_KEY: ${{ secrets.MDC_API_KEY }}
        run: uv run python src/dataset_schema_registry/sync_dataset_registry.py

      - name: Commit updated registry JSON
        run: |
          git add docs/dataset_registry.json
          git diff --cached --quiet || git commit -m "chore: update dataset registry [skip ci]"
          git push

  # Regenerates registry/index.json (the machine-readable index of which dataset
  # IDs have a schema) and the coverage page derived from it, then publishes the
  # docs.  Runs on every merge to main, so the index is current the moment a new
  # schema lands.
  index-and-docs:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    needs: [sync-registry]
    if: >-
      always() && (
        needs.sync-registry.result == 'success' ||
        needs.sync-registry.result == 'skipped'
      )
    steps:
      - name: Check out the repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Configure git
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'

      - name: Install requirements
        run: uv sync --group docs

      - name: Pull latest commits (registry JSON may have just been pushed)
        run: git pull --rebase origin main

      - name: Generate registry index
        run: uv run python src/dataset_schema_registry/generate_index.py

      - name: Generate dataset coverage page
        run: uv run python src/dataset_schema_registry/generate_coverage.py

      - name: Commit updated index and coverage page (push / schedule / dispatch only)
        if: ${{ github.event_name != 'pull_request' }}
        run: |
          git add registry/index.json docs/dataset-coverage.md
          git diff --cached --quiet || git commit -m "chore: update registry index and dataset coverage [skip ci]"
          git push

      - name: Build docs
        if: github.event_name == 'pull_request'
        run: uv run mkdocs build -s

      - name: Publish docs
        if: ${{ github.event_name != 'pull_request' }}
        run: uv run mkdocs gh-deploy
